All six files read. Here is the report.

---

# FABLE ONE-SHOT ADVERSARIAL REVIEW — `core-reverse-engineering-loop.sh`

**Bottom line first:** the loop's verdict string — "math axioms hold at bit level, machine code present, deterministic" (`core-reverse-engineering-loop.sh:87`) — is supported by **8 greps and 4 Python tautologies that never call the Rust kernel**. The "8/8 GREEN" of the doc counts only the greps: the cargo-test ✓ is printed by `awk`, not `ok()` (`:41`), and no Python axiom touches the `PASS`/`FAIL` counters at all. The loop is the third enforcement script to commit the exact meta-fallacy the catalog's bottom line names: *it verifies labels, not properties* (`FABLE-FALLACY-CATALOG.md:36-40`).

## A. What it ACTUALLY proves vs what it CLAIMS

| Check | Claims (doc/stdout) | Actually proves |
|---|---|---|
| P1 | "disassembles the five math primitives… proves the kernel ships real machine code, not a stub" (`doc:11-13`); "present in machine code (disassemblable)" (`sh:28`) | The symbol **name** appears as a string in objdump output — or, on fallback, **anywhere in the raw binary** via `grep -aq` (`sh:31`). A stub `fn sinc(_) -> f64 { 0.0 }` passes. Content is never checked; the `head -14` dump (`sh:36`) is cosmetic and `\|\| echo`-swallowed. |
| P1 build | "Builds rust-core… to wasm32" (`doc:9`) | The script **never builds**; it requires a pre-existing artifact (`sh:23`). The wasm may be stale — a different binary than the source P2's `cargo test` compiles natively. The two halves of the verdict describe two different binaries. |
| P2 cargo | "bit-level fundamental math axioms" | That the kernel's own tests pass (`sh:41`). Self-testimony, uninspected: delete `test_sinc_singularity_and_zero` (`lib.rs:1039`) and the loop stays GREEN. |
| P2 Python | "Independent bit-level re-derivation… ground-truth, not trusting Rust" (`sh:42`) | That Python can evaluate arithmetic on constants. **No line of Python calls Rust or the wasm.** |
| P3a | "single-instance process-global CSR (ABI contract stated)" (`sh:69`) | That the string `PROCESS-GLOBAL` occurs in a comment **in a different crate** (`field.rs:14`). |
| P3b | "ZERO RNG/clock/network leak (pure, air-gapped)" (`sh:79`) | That 6 specific tokens don't appear on lines containing no `*` and no `//` (`sh:75-76`). |
| P3c | "primitives are #[no_mangle] extern C" (`sh:82`) | That **at least one** `#[no_mangle]` exists anywhere in the file. Neither the 5 primitives nor `extern "C"` are checked. |
| P4 | "Constant-time / no secret-dependent branch check" (`doc:22`) | **Does not exist.** There is no such check anywhere in the script. The doc claims a check that was never written. |

## B. Fallacies & biases (each with follow-up in §D)

**F1 — FALSE PRECISION / CIRCULARITY: the Python "axioms" are tautologies that never touch bebop** (`sh:43-63`). Axiom A is `1.0 if abs(0.0) < 1e-9 else …` — the condition is constant-true, so it asserts `1.0 == 1.0` (`sh:49-50`); it re-implements the Rust guard (`lib.rs:599`) instead of testing it. Axiom C computes `a[1]*a[2]-a[2]*a[1]` — literally `x−x` by construction (`sh:58`). Axiom B computes `dot/(n·n)` where `dot = n²` (`sh:55-56`). Axiom D asserts one dot product is 0 while its label claims "bilinear + commutative" — neither is tested (`sh:60-62`). The `f64()` bit-packing helper — the only thing in the block that is "bit level" — is **defined and never called** (`sh:45`). Comparisons use `1e-12` tolerances, not bit equality; no NaN case exists despite the doc claiming "IEEE-754 / NaN / 0-edge axioms proven at the bit level" (`doc:14`).

**F2 — CONFIRMATION BIAS, STRUCTURAL: Axiom A has no RED path into the verdict.** Its failure is caught by `try/except`, prints `✗`, and increments **nothing** (`sh:48-53` vs. counters at `sh:19-20`); the script would still print `VERDICT: GREEN` (`sh:87`) with a ✗ on screen. Axioms B–D use bare `assert`, so failure kills the script via `set -e` (`sh:13`) with no verdict line at all — crash-closed, never RED. Since F1 makes them tautologies anyway, **no kernel bug can ever turn P2's Python into a RED**.

**F3 — LABEL-WITHOUT-PROPERTY with a live substring false-match in P1.** `grep -q "field_build"` matches `field_build_f32` (`lib.rs:78`): delete `field_build` itself and P1 stays GREEN (`sh:26-27`). The fallback `grep -aq "$sym" "$WASM"` (`sh:31`) searches the whole binary — a string constant in a data segment passes — while stdout overclaims "present in wasm export table" without parsing any export table. And when no wasm-capable objdump exists (`OBJ` falls through to GNU `objdump`, `sh:17`, which can't read wasm; stderr discarded at `sh:27`), **all five checks silently degrade to raw byte-grep** while printing the stronger claim.

**F4 — NAMED-BLIND-SPOT, REPEATED AND WIDENED: the P3b comment filter excludes every line containing `*` or `//`** (`sh:76` — the ERE `//|/\*|\*` has a bare-`*` alternative). Concrete false negatives: `let t = SystemTime::now(); // seed` (trailing comment) and `use rand::prelude::*;` (glob import) are both invisible. Any leak with a multiplication on the same line passes. The doc celebrates the RED self-catch (`doc:33-36`), but the fix for one false positive created a *family* of false negatives — the loop's only demonstrated RED was a bug in the harness, and its remedy broadened the blind spot. The regex also omits `Instant::now`, `std::env`, `getrandom`, `UdpSocket` — while the verdict says "ZERO RNG/clock/network" (`sh:79`) and the sibling doc claims `std::env` was covered (`reverse-engineering-loop-2026-07-10.md:18-19`).

**F5 — LABEL-WITHOUT-PROPERTY, AND THE PROPERTY IS FALSE: P3a greps a comment; the claim it stands for is factually wrong.** The script asserts "process-global graph is the ONLY mutable global" (`sh:11`), verified by grepping the word `PROCESS-GLOBAL` in `field.rs` (`sh:68`, hit at `field.rs:14`). Reality: rust-core has **two** mutable globals — `STATE` (`lib.rs:37`) and `ACCUM` (`lib.rs:48`) — and `ACCUM` carries Δu history **across calls**, making `field_sensitivity` output depend on call history (`lib.rs:434-453`): same input, different output. Bonus: `GraphState.field_energy` is documented as "accumulated |Δu| history" (`lib.rs:31-35`) but is only ever initialized (`lib.rs:67,97`), never accrued, never read — dead state whose doc-comment claims a property the code doesn't have. The comment-grep can see none of this; it passes on stale docs and fails on reworded correct ones.

**F6 — CIRCULARITY in P2's cargo call.** `cargo test -p bebop-core` (`sh:41`) is the kernel testifying for itself; the loop never verifies which tests ran. Also a bookkeeping lie: the ✓ is printed by `awk`, not `ok()`, so cargo success is excluded from `PASS` — the "8/8" the doc reports (`doc:36`) is **exactly the 8 greps** (5×P1 + P3a/b/c). Every check that is even nominally empirical is outside the count the verdict is drawn from.

**F7 — EQUIVOCATION, three counts.** (1) "bare-metal machine code (Wasm)" (`sh:3-4`, `doc:9`): Wasm is portable VM bytecode executed under a host runtime — nothing here is bare metal, and the wasm is **never executed** by the loop. (2) "air-gapped" (`sh:79`) names an environmental property but is concluded from token absence in source. (3) "deterministic": beyond F5's history-dependence, the kernel's own test comment admits `STATE` can be "overwritten by a sibling thread mid-run" (`lib.rs:840-841`) — the Mutex prevents data races, not interleaving-dependence; determinism is delegated to caller discipline the loop never checks.

**F8 — COMPOSITION.** Verdict (`sh:87`): 5 name-greps + 3 doc/attribute-greps + kernel self-tests + 4 Python tautologies ⇒ "math axioms hold at bit level, machine code present, deterministic." Even if every part were sound, the conjunction establishes only: names present, one attribute present, six tokens absent, identities of *textbook definitions* hold in Python. Kernel soundness does not follow.

**F9 — DOC-CLAIM DRIFT (catalog C3/C10, recommitted).** The doc claims a P4 constant-time check that doesn't exist (`doc:22`); claims "Wasm text + raw bytes" extraction for five primitives vs. an optional 14-line `head` for `sinc` only (`doc:11` vs `sh:34-36`); the script header says "the 4 exported primitives" while looping over 5 (`sh:8` vs `sh:26`); and the test counts drift across the trio — 294 (`doc:40`), 293 (`reverse-engineering-loop:72`), 292/293 (catalog C3/C10 row) — the same drift pattern the catalog already flagged as open.

**F10 — CLAIMED COVERAGE CONTRADICTS AN OPEN CATALOG FINDING.** Catalog **C8** is open: `fexp` range-reduction bound `|r|≤ln2/2` is false for negative arguments (`FABLE-FALLACY-CATALOG.md:30`) — verifiable in `lib.rs:489` via `fround(x)=ftrunc(x+0.5)` (`lib.rs:522-524`), which rounds negatives wrong (e.g. `fround(-0.7)=0`, giving `|r|≈0.485 > 0.347`). `fexp`/`fcos` are the functions `spectral_propagate` actually depends on for every Chebyshev coefficient (`lib.rs:206-207`), yet P2's "axioms" never touch them. Stamping "math axioms hold at bit level" over a kernel with a known-open false numeric invariant in its hottest math path is claiming coverage the loop does not have.

## C. Patterns — where the loop breaks its own falsifiability discipline

- **C-1 Unfalsifiable checks presented as falsifiable.** Axioms B/C/D can only fail if Python's arithmetic is broken (F1); P1 can fail only on "name missing," never on "function wrong" (F3). "Every claim is a falsifiable assertion with a RED path" (`sh:5`) is itself a label applied faster than the property is established — §0·GP verbatim.
- **C-2 GREEN is structural, not empirical.** Of the three clauses in the verdict string, only "machine code present" maps to counted checks, and those are name-greps. "Axioms hold" and "deterministic" have zero counted, kernel-touching evidence behind them.
- **C-3 The harness ignores the catalog it claims to twin.** The doc calls this loop the "deterministic, deep, total-scope twin of the fable adversarial review" (`doc:41-43`), but it neither re-verifies the CLOSED items (B4/B11/C2/B8 — all in `crates/bebop`, out of its scope) nor gates on the OPEN rust-core item C8 that is squarely *in* its scope.
- **C-4 The only RED in the loop's history was self-inflicted, and the fix regressed coverage** (F4). A harness whose sole falsification event is a harness bug, repaired by weakening the check, has demonstrated fail-closed *plumbing* but not fail-closed *checking*.

## D. Deterministic follow-ups

- **D1 (F1/F2):** Replace the Python block with a harness that calls the **built artifact**: run `bebop_core.wasm` under wasmtime (or build a native cdylib) and assert bit-exact via the existing `f64()` helper — `struct.pack('<d', sinc(0.0)) == struct.pack('<d', 1.0)`, `cosine(v,v)` bit-equals 1.0, `cosine(0-vector, v) == 0.0` (the real edge guard, `lib.rs:571-575`), plus a NaN-propagation case. Make Python exit nonzero on any failure and wire it `python3 … || bad "axioms"` so the RED reaches the verdict, not just the screen.
- **D2 (F3):** Parse the actual export section: `wasm-objdump -x -j Export "$WASM" | grep -qw "$sym"` (word-anchored — kills the `field_build`/`field_build_f32` false match). Hard-fail (`bad`) when no wasm-capable objdump exists instead of silently degrading. Build the wasm **inside** the loop (as the doc already claims) or `bad` on artifact-older-than-source mtime.
- **D3 (F4):** Delete the grep-with-comment-filter. Two property-level replacements: (a) `cargo clippy -p bebop-core -- -D clippy::disallowed-methods` with `SystemTime::now`, `Instant::now`, `thread_rng` listed in `clippy.toml`; (b) the machine-code-level check the loop's own framing demands: **assert the wasm Import section is empty** (`wasm-objdump -x -j Import` → no entries) — a wasm module with no imports *cannot* reach a clock, RNG, or socket. That single check is stronger than the entire current P3b.
- **D4 (F5):** Replace the comment-grep with `grep -c '^static .*Mutex' rust-core/src/lib.rs` and assert the count equals an explicit allowlist (`STATE`, `ACCUM`). Retract "the ONLY mutable global" (`sh:11`) or amend to name both. Separately: either wire `field_energy` accumulation or delete the field and its false doc-comment (`lib.rs:31-35`).
- **D5 (F6):** Assert the named axiom tests ran: grep `cargo test` output for `test_sinc_singularity_and_zero … ok`, `test_cosine_similarity_bounds … ok`, `test_cross_product_orthogonality … ok`; `bad` on absence (protects against test deletion). Route the cargo ✓ through `ok()` so `PASS` counts it.
- **D6 (F9):** Either implement P4 (constant-time grep) or delete `doc:22`; renumber the doc's P-labels to match the script; fix "4 exported primitives" (`sh:8`); reconcile the 292/293/294 counts and add them to the doc-claims gate (the catalog's own C3/C10 follow-up).
- **D7 (F10):** Before the verdict may say "math axioms hold": add the catalog-C8 RED test (`fexp(-13.2)` against a reference value; assert the `|r|≤ln2/2` invariant or fix `fround` for negatives) and a `fexp`/`fcos`-vs-reference sweep — these are the axioms the propagator actually rests on.
- **D8 (F7):** Retract the equivocations in `sh:3-4,79` and `doc:9`: "bare-metal machine code" → "Wasm bytecode"; "air-gapped" → "no network APIs referenced"; "deterministic" → "deterministic per call-history; `field_sensitivity` is stateful across propagations." If D3(b) lands, "cannot import clock/RNG/network" becomes an honest, machine-checked replacement for all three phrases.

**Verdict on the verifier:** as it stands, `core-reverse-engineering-loop.sh` would print GREEN for a kernel whose five primitives are stubs, whose axiom tests were deleted, and whose only leak is `SystemTime::now(); // seed`. The highest-leverage single fix is D3(b) — the empty-import-section check — because it is the one place where the loop's "machine code" rhetoric can be converted into an actual machine-code property.
FABLE_CORE_RE_LOOP_EXIT=0
