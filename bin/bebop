#!/usr/bin/env bash
# bebop CLI shim — native Rust binary entry point.
# Builds (if needed) and runs the bebop agent. No TypeScript in the runtime path.
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo run -q -p bebop -- "$@"
